# openclaw-voice environment variable reference
# Copy to .env.local (never commit .env.local) and adjust to your setup.
#
# All variables use the OPENCLAW_VOICE_* prefix.
# CLI flags take priority over env vars; env vars take priority over TOML config.

# ---------------------------------------------------------------------------
# Debugging & Logging
# ---------------------------------------------------------------------------

# Set to "true" to enable verbose DEBUG-level structured JSON logging.
# Equivalent to passing --log-level DEBUG on the CLI.
# NEVER commit .env.local with this enabled.
OPENCLAW_VOICE_DEBUG=false

# Override the log level explicitly (DEBUG|INFO|WARNING|ERROR).
# Ignored if OPENCLAW_VOICE_DEBUG=true.
OPENCLAW_VOICE_LOG_LEVEL=INFO

# ---------------------------------------------------------------------------
# Config file
# ---------------------------------------------------------------------------

# Path to the TOML config file (lowest priority; CLI flags override it).
# OPENCLAW_VOICE_CONFIG=/etc/openclaw-voice/config.toml

# ---------------------------------------------------------------------------
# STT Bridge (Wyoming ↔ whisper.cpp)
# ---------------------------------------------------------------------------

# Host and port for the Wyoming STT TCP server.
OPENCLAW_VOICE_STT_HOST=0.0.0.0
OPENCLAW_VOICE_STT_PORT=10300

# whisper.cpp HTTP inference endpoint.
OPENCLAW_VOICE_WHISPER_URL=http://localhost:8001/inference

# Enable speaker identification for every transcription.
# OPENCLAW_VOICE_SPEAKER_ID=true

# URL of the Speaker ID /identify endpoint (used when speaker ID is enabled).
OPENCLAW_VOICE_SPEAKER_ID_URL=http://localhost:8003/identify

# Path to append JSONL transcript logs (leave unset to disable).
# OPENCLAW_VOICE_TRANSCRIPT_LOG=/var/log/openclaw-voice/transcripts.jsonl

# ---------------------------------------------------------------------------
# TTS Bridge (Wyoming ↔ Kokoro)
# ---------------------------------------------------------------------------

# Host and port for the Wyoming TTS TCP server.
OPENCLAW_VOICE_TTS_HOST=0.0.0.0
OPENCLAW_VOICE_TTS_PORT=10200

# Kokoro TTS OpenAI-compatible endpoint.
OPENCLAW_VOICE_KOKORO_URL=http://localhost:8002/v1/audio/speech

# Default Kokoro voice (native name or OpenAI alias).
# Kokoro natives: af_heart, af_nova, af_sky, am_adam, am_michael, bf_emma
# OpenAI aliases: alloy, echo, fable, onyx, nova, shimmer
OPENCLAW_VOICE_DEFAULT_VOICE=af_heart

# ---------------------------------------------------------------------------
# Speaker ID Server (Resemblyzer)
# ---------------------------------------------------------------------------

# Host and port for the Speaker ID HTTP server.
OPENCLAW_VOICE_SPEAKER_ID_HOST=0.0.0.0
OPENCLAW_VOICE_SPEAKER_ID_PORT=8003

# Directory where speaker profile JSON files are stored.
OPENCLAW_VOICE_PROFILES_DIR=./speaker-profiles

# Inference device for Resemblyzer ("cpu" or "cuda").
OPENCLAW_VOICE_RESEMBLYZER_DEVICE=cpu

# ---------------------------------------------------------------------------
# Discord Voice Bot
# ---------------------------------------------------------------------------

# Discord bot token — obtain from https://discord.com/developers/applications
# NEVER commit this value. Set in .env.local only.
OPENCLAW_VOICE_DISCORD_TOKEN=

# Guild (server) ID for slash command registration.
# If unset, commands register globally (takes up to 1 hour to propagate).
OPENCLAW_VOICE_GUILD_ID=

# LLM OpenAI-compatible endpoint for the voice pipeline.
OPENCLAW_VOICE_LLM_URL=http://localhost:8000/v1/chat/completions

# LLM model name to use.
OPENCLAW_VOICE_LLM_MODEL=Qwen/Qwen2.5-32B-Instruct
